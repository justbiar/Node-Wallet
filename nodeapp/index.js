//Console kısmına içerideki yazısı yazar. 
//console.log("Hello World");

//Değişken tanımlama
/*
let name = 'Biar';
console.log(name);



// Cannot be a reserved keyword (e.g. if, else etc.)
// Shold be meaningful
// Cannot start with a number (1name)
// Cannot contain a space or hyphen (-)

let firstName = 'Biar'; 
let lastName = 'Arf';
// Primitives / Valuse types
let name = 'Biar'; // String literal
let age = 30; // Number literal
let isApproved = false; // Boolean literal
let firstName = undefined;
let selectedColor = null;


// Reference Types

let person = {
    name: 'Biar',
    age: 30
};

console.log(person);

let selectedColor = ['red', 'blue'];
selectedColor[2] = 'green'; // üçüncü renk olarak green eklendi
console.log(selectedColor[0])
// [0] birinci olanı ekrana yazdırır. 


//function

function greet() {
    console.log('Hello World');
}

greet();



function greet(name, lastName) {
    console.log('Hello ' + name + ' ' + lastName);
}

greet('John', "Smith");

*/

// Calculating a value 

function square(number) {

    return number * number;
}

console.log(square(2));
